services:
  dashboard:
    build: .
    env_file:
      - path: .env
        required: false
    environment:
      - TZ=America/Los_Angeles
    volumes:
      - chatgpt_data:/data
    networks:
      - private
    restart: unless-stopped

volumes:
  chatgpt_data:

networks:
  private:
    external: true
